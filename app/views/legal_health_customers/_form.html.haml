= form_for @legal_health_customer do |f|
  - if @legal_health_customer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@legal_health_customer.errors.count, "error")} prohibited this legal_health_customer from being saved:"
      %ul
        - @legal_health_customer.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
